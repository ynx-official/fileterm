# FileTerm 1.2.1

## Windows compatibility

- 修复 SSH 连接配置保存后，已打开标签页没有同步“回车重连”设置，导致 Windows 用户按回车无响应的问题。
- 加固断线终端和底部命令输入框的回车重连入口，兼容不同平台的回车/CRLF 输入事件。
- 修复文件区域获得焦点时出现白色外框的问题。
- 恢复并明确终端输入快捷键提示：macOS 使用 `⌥` 打开历史、双击 `Cmd` 切换输入区域；Windows/Linux 使用 `Alt` 打开历史、双击 `Ctrl` 切换输入区域。
